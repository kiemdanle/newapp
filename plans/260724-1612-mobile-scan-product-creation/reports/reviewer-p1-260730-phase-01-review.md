# Phase 1 Review — Contracts and Data Model (commit `1a42b0c`)

Reviewer: reviewer-p1. Branch `feature/mobile-scan-product-creation`. Review-only; no source files modified.

## Verification actually run (not taken from the report)

| Check | Command | Result |
|---|---|---|
| API suite | `pnpm --dir api test` | 83 files, **431/431 pass** (72s) — claim confirmed |
| Shared suite | `pnpm --dir packages/shared test` | **51/51 pass** — claim confirmed |
| Mobile contract | `pnpm --dir apps/mobile test -- shared-contract` | **5/5 pass** |
| Typecheck | api / packages/shared / apps/admin `tsc --noEmit` | all clean, exit 0 |
| Vendored dist drift | `tsc -p tsconfig.build.json --outDir /tmp/sharedbuild` + `diff -rq` vs `apps/mobile/local-packages/@expyrico/shared/dist` | **no `.js`/`.d.ts` differences**; only `.map` sourceRoot (temp outDir artifact). No compiled `*.test.js` in dist |
| Migration B unapplied | read-only `SELECT migration_name FROM _prisma_migrations` on `pantry_test` | only `..._expand_product_lifecycle_enums` + `..._expand_product_drafts_photos_and_moderation` recorded; **`20260730040000_classify_report_hidden_products` absent**. No `migrate deploy/dev/reset` run by me; shared `pantry` DB never connected to |
| Secrets/paths | `git show 1a42b0c \| grep -E "(/home/\|/opt/newapp\|PRIVATE KEY\|password=\|secret=\|api[_-]?key)"` | no hits; `api/.env.test` not in the commit |
| Lockfile | `git show 1a42b0c -- pnpm-lock.yaml` | 3 lines: vitest `^2.0.0` devDep for `packages/shared` (same range as `api`) |
| DB constraint probes | rolled-back transactions against `pantry_test` (see MODERATE-7) | reorder semantics confirmed empirically |

---

## CRITICAL

**[CRITICAL] The admin console's product-edit "Reject" action is broken in production by this commit.** — `packages/shared/src/schemas/admin/products.ts:80-90` narrows `decision` to `'approve' | 'request_changes'` and adds `.refine(notes required when request_changes)`, but every caller still sends `'reject'` with no notes: `apps/admin/src/app/(admin)/products/pending/pending-actions.tsx:22` → `apps/admin/src/lib/actions.ts:36-42` → `apps/admin/src/lib/admin-api.ts:86-89` → `PATCH /v1/admin/products/pending/:id`, which parses the schema at `api/src/routes/admin/products/pending-resolve.ts:12`. Every reject now returns 400 `validation_error`. This passes CI because (a) the admin call sites use hand-written string-literal unions, not the inferred shared type, so `apps/admin` typecheck stays green, and (b) the only API test for this route sends `decision: 'approve'` (`api/tests/integration/admin/products.test.ts:103`) — there is no negative/reject case. Naive fix is also broken: renaming the admin literal to `request_changes` without adding notes still 400s on the new `.refine`. — **Recommendation:** in the same PR as this contract change, update the three admin call sites to send `request_changes` **plus** a notes string (prompt or default), or temporarily accept `'reject'` as an alias in `adminProductEditResolveSchema` and add an API test for the non-approve branch. The phase file mandated the rename; it did not authorize leaving the only caller broken for 5 phases.

## IMPORTANT

**[IMPORTANT] `request_changes` still writes the terminal `rejected` state.** — `api/src/routes/admin/products/pending-resolve.ts:24`: `status: input.decision === 'approve' ? 'approved' : 'rejected'`. Phase file requirement (line 27) is explicit: "Admin action is named `request_changes`; resulting state is `changes_required`… The only new write to `rejected` is an explicit stale-revision `supersede`". So once CRITICAL-1 is fixed, the first request-changes action writes an irreversible, read-only-by-spec `rejected` row instead of a resumable `changes_required` one. The enum value `changes_required` exists in the DB (migration A1) and in `productEditStatusSchema`, so nothing but the route stands in the way. — **Recommendation:** map `request_changes → changes_required` and persist `moderationNotes` (the new column) rather than the legacy `notes` column, or explicitly defer with a code-level guard that rejects `request_changes` with `501/upgrade_required` until Phase 4 owns the writer. Do not leave the accepted input silently producing the wrong terminal state.

**[IMPORTANT] Product-level moderation notes are exposed to every caller in the public product DTO.** — `api/src/services/products/serializer.ts:39` maps `moderationFeedback: p.moderationNotes` unconditionally, and `moderationFeedback` is a required field of the *public* `productSchema` (`packages/shared/src/schemas/product.ts:95`). `toApiProduct` feeds four public routes: `routes/products/get.ts:23` (any authenticated user, any product id), `routes/products/lookup.ts:32`, `routes/products/search.ts:17`, `routes/products/create.ts:27`; it is also embedded in `problemSchema.canonicalProduct` (`packages/shared/src/schemas/error.ts:21`), so it rides along in `version_conflict` problems too. The behaviour is asserted as intended in `api/tests/unit/products-serializer.test.ts:66-72` (`expect(api.moderationFeedback).toBe('Blurry cover photo')`). This contradicts the phase's own success criterion "Public DTOs never expose storage keys/uploader/moderation internals" (phase file line 203). Exposure is currently *latent* — `products.moderation_notes` is new in migration A2 and no writer exists yet — but the leaking shape is now baked into the shared contract and the vendored mobile dist, and Phase 4 will start populating the column. — **Recommendation:** remove `moderationFeedback` from `productSchema` and surface it only on creator-private projections (`productDraftRowSchema` already carries it), or pass an explicit `viewerIsCreator`/`includeModerationFeedback` argument into `toApiProduct` and default it to omitted. Add a serializer test asserting a non-creator projection contains no `moderationFeedback`.

**[IMPORTANT] `productDraftPatchRequestSchema` converts an omitted `description` into an explicit `null` — silent data loss on partial patches.** — `packages/shared/src/schemas/product.ts:40-56` (`productDescriptionSchema` = `.nullable().optional().transform(...)`) and `:229` use it directly as the object field. Verified with the repo's own zod build: `parse({ name: 'x' })` → `{"name":"x","description":null}`; `parse({})` → `{"description":null}`; `'description' in parsed === true`. Consequences: (1) a Phase 2 handler that spreads the parsed patch into a Prisma update — the pattern used at `api/src/routes/products/patch.ts:33` — wipes an existing description whenever the client patches only `name`; (2) the existing empty-payload guard idiom (`Object.keys(input).length === 0`, `patch.ts:13`) can never trigger, so an empty `{}` body becomes a destructive write. No test covers the omitted-key case (`packages/shared/src/schemas/product.test.ts:282-292` only tests a supplied description). — **Recommendation:** split the schema: keep a `productDescriptionValueSchema` (`string | null` → normalized) for required-value contexts and use `productDescriptionValueSchema.optional()` inside the patch object so an omitted key stays `undefined`. Add tests for omitted key, explicit `null`, and `{}`.

**[IMPORTANT] The one-open-edit invariant fails open: `is_legacy` defaults to `true`.** — `api/prisma/migrations/20260726160100_.../migration.sql` adds `"is_legacy" BOOLEAN NOT NULL DEFAULT true` and the partial unique index is `WHERE (NOT "is_legacy" AND status = ANY(...))`; `api/prisma/schema.prisma` mirrors it with `@default(true)`. The default that *disables* the constraint applies to every future insert, not just the backfill. The live writer `api/src/routes/products/patch.ts:29-35` creates `ProductEdit` rows without `isLegacy`, so every edit it creates is exempt and one user can open unlimited concurrent `pending` edits on one product. The integration tests only prove the constraint when `isLegacy: false` is passed explicitly (`api/tests/integration/products-schema.test.ts:389, 413, 424`), so the gap is invisible. Phase success criterion "Database enforces … one-open-edit race invariants" is therefore only met for writers that remember to opt in. — **Recommendation:** follow-up migration doing `ALTER TABLE product_edits ALTER COLUMN is_legacy SET DEFAULT false` (backfill already done) plus `@default(false)` in Prisma, so omission fails closed; set `isLegacy: false` in `patch.ts`. Add a test that inserts two open edits **without** specifying `isLegacy` and expects the second to fail.

**[IMPORTANT] Migration B's preflight implements one of the three mandated aborts.** — `api/prisma/migrations/20260730040000_classify_report_hidden_products/migration.sql` only aborts on `status='pending' AND submitted_at IS NOT NULL`. Phase file line 166 requires abort "if creation mode is not `off`, any submission timestamp/private draft lifecycle marker exists on such a row, or any deployed report writer still emits `pending`". The creation-mode precondition **is** DB-checkable — migration A2 inserts `settings('product_creation','{"mode":"off"}')` — so the file's comment ("the one precondition that IS DB-checkable") is factually wrong. Other DB-checkable markers on a candidate row are also unchecked: `moderated_at`/`moderated_by_user_id`, `version > 1`, any `product_photos` row, any non-legacy `product_edits` row. Failure mode is silent, irreversible data corruption: a creator-submitted `pending` product flipped to `report_hidden` disappears from its creator's drafts with no audit trail. — **Recommendation:** extend the `DO` block to also `RAISE EXCEPTION` when `(SELECT value->>'mode' FROM settings WHERE key='product_creation') IS DISTINCT FROM 'off'` and when any candidate row carries a lifecycle marker; add integration cases for each new abort next to `api/tests/integration/products-schema.test.ts:471`.

## MODERATE

**[MODERATE] Photo reorder cannot be expressed as an `UPDATE` — the position constraints leave no legal intermediate state.** — `product_photos_product_id_position_key` is a plain (immediate, non-`DEFERRABLE`) UNIQUE and `product_photos_position_check` pins `position` to 0–4; `product_edit_photos` has the same pair. Verified empirically in rolled-back transactions against `pantry_test`: a single-statement swap `UPDATE product_photos SET position = CASE position WHEN 0 THEN 1 ELSE 0 END WHERE product_id = …` fails with SQLSTATE **23505**, `Key (product_id, "position")=(…, 1) already exists`; and the usual escape hatch `UPDATE … SET position = 5` fails `violates check constraint "product_photos_position_check"`. The plan promises a "transactional contiguous-order rewrite" (plan.md risk table) and Phase 1 owns these constraints. — **Recommendation:** decide now and record it in the Phase 2/3 phase files: either make the unique constraints `DEFERRABLE INITIALLY IMMEDIATE` (+ `SET CONSTRAINTS … DEFERRED` in the reorder transaction) or mandate delete-then-reinsert of the whole ordered set. Discovering this inside Phase 3 will cost a migration.

**[MODERATE] The serializer never orders `photos` by `position`.** — `api/src/services/products/serializer.ts:40` maps `p.photos ?? []` in whatever order the Prisma include returned; Prisma applies no default `orderBy`, so row order is unspecified. `position 0 = cover` is contractual (plan.md Global Constraints). The test at `api/tests/unit/products-serializer.test.ts:104-110` only proves array order is preserved, not that it is correct. — **Recommendation:** sort by `position` inside `toApiProduct` so no caller can get cover order wrong.

**[MODERATE] `productSchema.photos` is looser than the DB and the spec.** — `packages/shared/src/schemas/product.ts:97` is `z.array(productPhotoSchema)` with no `.max(5)` and no unique-position rule, while the DB enforces `position 0..4` + `UNIQUE(product_id, position)` and the plan caps at five ordered photos. A buggy or malicious writer can emit six photos or duplicate positions and the response contract will accept it. — **Recommendation:** `.max(5)` plus a `superRefine` for unique positions.

**[MODERATE] The Phase 1 "compatibility reader" step is unimplemented and untested.** — Phase file line 165 requires that legacy `pending` and new `report_hidden` rows be "treated as hidden by every Phase 1-touched serializer/consumer". `api/src/services/products/lookup.ts:17-23` (`findUnique` on `barcode`/`qrPayload`) and `api/src/routes/products/get.ts:13` (`findUnique` by id) apply no status filter, so `report_hidden`, `draft`, and `pending` rows serialize fully to any authenticated caller; only `services/products/search.ts:40` filters `status = 'active'`. This is **not a regression** (legacy `pending` already behaved this way and neither file is touched by the commit) and plan.md assigns "active-only legacy lookup" to Phase 2 — but nothing in Phase 1 asserts the hidden-ness the rollout gate depends on. — **Recommendation:** treat this as a Phase 2 blocking item that must land *before* the report-writer switch to `report_hidden`, with a test proving `report_hidden` is invisible via `/v1/products/lookup` and `/v1/products/:id`.

**[MODERATE] A `rejected` photo can never have its storage key cleared.** — `product_photos_storage_and_moderation_check` requires `private_storage_key IS NOT NULL` for `pending|rejected`. A cleanup worker that deletes rejected private bytes cannot null the key afterwards without violating the check, so a swept row would permanently reference a nonexistent object. — **Recommendation:** document in Phase 3/7 that rejected-photo cleanup deletes the **row** (and enqueues `delete_private`), never just the key.

**[MODERATE] Admin↔API deploy order is now coupled and undocumented.** — `adminProductEditRowSchema` gained six required fields (`version`, `baseProductVersion`, `moderationNotes`, `submittedAt`, `resolvedBy`, `resolvedAt`, `packages/shared/src/schemas/admin/products.ts:69-77`) and `apps/admin/src/lib/admin-api.ts:83-85` parses `adminProductEditsListSchema` on every `/admin/products/pending` render. An admin build carrying the new shared package against an API instance predating this commit throws on the pending page. Verified this is the *only* such coupling: no other `apps/admin`/`apps/mobile` source parses `productSchema`, `productWithReviewsSchema`, `productLookupResponseSchema`, or `productSearchResultSchema`, so the new required product fields break no existing client. — **Recommendation:** add "deploy API before admin" to the plan's Rollout Strategy step 2.

**[MODERATE] Uploader FKs are `RESTRICT` while the product creator FK is `SET NULL`.** — `product_photos.uploaded_by_user_id` and `product_edit_photos.uploaded_by_user_id` are `ON DELETE RESTRICT` (migration A2; `schema.prisma`), whereas `products.created_by_user_id` is `SET NULL`. No hard-delete path exists today (verified: no `prisma.user.delete` anywhere in `api/src` or `apps/admin/src`; admin user routes are get/list/patch/impersonate/revoke-sessions only), so nothing is broken now — but any future account-erasure/GDPR path will be blocked by a photo upload. — **Recommendation:** Phase 7 should decide `SET NULL` + retained-uploader audit vs. an explicit erasure procedure, and record it.

**[MODERATE] `api/prisma/schema.prisma` carries a whole-file `prisma format` reflow beyond the phase's scope.** — 162 whitespace-normalized diff lines touch `User`, `Record`, `Review`, `ReviewVote`, `ApiError`, `Deal`, `DealVote` — models this phase does not own. I verified the reflow is semantically inert (whitespace-normalized sorted diff yields only `@db.Uuid @map(...)` → `@map(...) @db.Uuid` attribute reorderings; no type, default, index, or relation change), so there is no correctness finding — but it violates `.claude/rules/development-rules.md` "keep changes scoped" and inflated the surface a reviewer must clear. — **Recommendation:** none required retroactively; avoid whole-file formatters on shared schema files in later phases.

---

## Acceptance criteria walk-through (phase file "Success Criteria")

| Criterion | Verdict |
|---|---|
| Shared tests real, not `echo skip` | **Met.** `packages/shared/package.json:17` → `vitest run`; 51 real assertions across 4 files; the description tests contain actual control bytes (they fail if stripped). Not phantom. |
| All outcome/error/status names representable and consistent | **Met.** Lookup v2 union, draft statuses, 6 new `ERROR_CODES`, `productCreationSettingsSchema` all present and exercised. |
| DB enforces position, XOR, retained-photo safety, outbox, one-open-edit | **Partially met.** Position/XOR/`RESTRICT`/outbox lease+payload/claim indexes are all real and constraint-violation-tested. One-open-edit fails open (IMPORTANT-4). |
| Legacy report-hidden classified separately before visibility changes | **Met with a gap.** Migration B exists, is unapplied (verified), is tested inside rolled-back transactions, and never renames an enum value — but its preflight is incomplete (IMPORTANT-5). |
| Photo/revision models cover every approved field + retained/staged order | **Met.** Every field in the phase Data Model section is present, including the `is_legacy` and `moderated_at`/`moderated_by_user_id` extras. |
| Public DTOs never expose storage keys/uploader/moderation internals | **Not met.** Storage keys/uploader/photo moderation are correctly redacted (`productPhotoSchema`, serializer test), but product-level `moderationNotes` is published to all callers (IMPORTANT-3). |
| Existing rows survive migration; shared/API/mobile gates pass | **Met.** Scratch-DB upgrade fixture (`products-schema.test.ts:511-587`) proves legacy `pending` products (with and without creator) and `pending|approved|rejected` edits survive A1+A2 unchanged with `is_legacy=true` backfill and default-off `product_creation`; all three suites and three typechecks re-run green by me. |

## Security boundary verdict

- `under_review` discloses nothing: `.strict()` object, and `packages/shared/src/schemas/product.test.ts:148-158` proves `productId`/`creator` are rejected. **Clean.**
- `canonicalProduct` is absent unless explicitly set, proven on the serialized form (`api/tests/unit/errors.test.ts:69-75`, `JSON.stringify` assertion). **Clean** — except that the embedded `productSchema` inherits IMPORTANT-3.
- Photo DTOs never carry `privateStorageKey`/`publicStorageKey`/`uploadedByUserId`/`moderationStatus`/`moderationNote`; `productDraftRowSchema.cover` is `.strict()` and rejects an injected `privateStorageKey`. **Clean.**
- No secrets, dotenv, media, or absolute local paths in the commit. **Clean.**

## Repo-hygiene observation (not from this commit)

`git status` shows an **uncommitted deletion of the entire `docs/superpowers/` tree**, including `docs/superpowers/specs/2026-07-24-mobile-scan-product-creation-design.md` — the approved design every phase file links as its context source. It is still present in `HEAD` (last touched by `ac2a486`), so nothing is lost yet, but any `git add -A` commit on this branch would erase the plan's source of truth. Recommend restoring or intentionally committing the removal before further phase work.

## Unresolved questions

1. Who fixes the admin `reject` call sites — this phase (out-of-ownership but 3 lines, unblocks a broken production action) or an expedited slice of Phase 6? Leaving it broken across Phases 2–5 is a live functional regression.
2. Should `moderationFeedback` be removed from `productSchema` now (breaks nothing — no client parses it yet) or carried until Phase 4 with a viewer-scoped serializer? Removing now is cheaper than after mobile consumes it.
3. Reorder strategy: `DEFERRABLE` unique constraints (needs a migration in this phase's territory) or delete-and-reinsert in Phase 2/3? Phase 1 owns the constraint, so the decision belongs here.

---

# Re-verification — commits `6c40854` + `d929a56`

Re-reviewed 2026-07-30 against the **commits**, not the working tree (dev-2 has concurrent uncommitted work in `routes/products/{lookup,drafts,draft-update,submit,lookup-v2}`, `services/products/{lookup,product-drafts,product-visibility}`, `plugins/idempotency.ts` — excluded from attribution below).

## How the test signal was obtained (the shared-DB problem is real)

Two full-suite runs against `pantry_test` under `flock /tmp/expyrico-api-test.lock` produced 33 and then 75 failures, all FK-violation/401 shapes (`product_edits_product_id_fkey`, `reports_reporter_id_fkey`, `expected 401 to be 200`) — the signature of another process truncating tables mid-test. `pgrep -fa vitest` confirmed a second vitest run (PIDs 1662438/1663534) alive during both; `flock` does not help because the other process does not take the lock.

To get an attributable result I provisioned my **own throwaway database** the same way `products-schema.test.ts` provisions its scratch DB — `psql -c 'CREATE DATABASE pantry_rev_…'`, `CREATE EXTENSION pg_trgm`, then `psql -f` over all 22 migration files in sort order — and ran the suite with the harness's supported `TEST_DATABASE_URL` override. **No `prisma migrate deploy/dev/reset` was run anywhere; `pantry` was only ever read.** The scratch DB was dropped afterwards (`DROP DATABASE pantry_rev_2m13lz2b`; remaining `pantry_*` databases on the box: `pantry_test`, `pantry_migration_rehearsal`).

| Check | Result |
|---|---|
| Full api suite, isolated DB | **555/558 pass**; 3 failures confined to `rate-limit.test.ts` + `idempotency.test.ts` (`expected 401 to be 429`, TTL assertion) |
| Those two files re-run alone, same DB | **12/12 pass** → the 3 failures are shared-**Redis** cross-process interference, not regressions |
| dev-1's five touched test files, isolated DB | **58/58 pass** (`products-schema` 29, `admin/products` 10, `products-serializer` 8, `products-patch` 5, `errors` 6) — matches dev-1's claim exactly |
| Shared suite | **57/57 pass** (was 51) |
| Mobile `shared-contract` | **5/5 pass** |
| Typecheck api / shared / admin | all clean (this includes dev-2's in-flight code, so no attribution ambiguity) |
| `prisma migrate status` (read-only) vs `pantry_test` **and** `pantry` | both: "22 migrations found… Database schema is up to date!" — **no pending migration, and no applied-but-missing-locally warning**, so relocating migration B left no dangling record |
| `_prisma_migrations` (read-only, both DBs) | `20260730040000_classify_report_hidden_products` **absent**; `20260730044500_make_photo_position_deferrable` applied (pantry 04:47, pantry_test) |
| Incident impact, independently confirmed | `pantry` products: `active=3`, **zero** `pending`/`report_hidden` rows → the accidental classify `UPDATE` matched zero rows. Matches the addendum. |

## Finding-by-finding

**[CRITICAL] admin "Reject" → 400 — CLOSED.** `apps/admin/src/lib/{actions,admin-api}.ts` now type `decision` as `AdminProductEditResolveDecision` (inferred from the schema, `packages/shared/src/schemas/admin/products.ts:100-104`), and `pending-actions.tsx` replaces the button with a two-step "Request Changes" + required-reason input that submits `('request_changes', trimmedNotes)` and disables Send while the reason is blank. `grep -rn "'reject'" apps/admin/src api/src packages/shared/src` returns only the negative tests. Three new API tests cover the previously untested branches: `request_changes` happy path, `request_changes` without notes → 400, legacy `reject` → 400 (`api/tests/integration/admin/products.test.ts:111-177`). The drift class is also closed structurally: `packages/shared/src/schemas/admin/products.test.ts:98` pins the union with `@ts-expect-error` on `'reject'`, so a future rename fails `tsc` (verified: shared typecheck passes with that line present).

**[IMPORTANT] `request_changes` wrote terminal `rejected` — CLOSED.** `pending-resolve.ts:24-30` now writes `changes_required` and persists `moderationNotes` (the new column) instead of legacy `notes`. Proven by `api/tests/integration/admin/products.test.ts:111-137`, which also asserts the proposal is **not** applied to the product. No reader of the vestigial `product_edits.notes` column exists (`grep` over `api/src` + `apps/admin/src`), so dropping the write breaks nothing; the column is now dead and can be removed in a later cleanup.

**[IMPORTANT] `moderationFeedback` leak in the public DTO — CLOSED.** Removed from `productSchema` (`packages/shared/src/schemas/product.ts:99-106`) and from `toApiProduct` (`serializer.ts:38-40`, with a "never map `p.moderationNotes` here" note). Tests assert absence on the serialized form (`products-serializer.test.ts` "never exposes moderationNotes as moderationFeedback", `JSON.stringify` check) and that a stray key is stripped by the non-strict schema (`product.test.ts:72-81`). Because `problemSchema.canonicalProduct` embeds `productSchema`, the `version_conflict` leak path is closed with it. Creator-scoped feedback still exists exactly where it belongs — `productDraftRowSchema.moderationFeedback` — and dev-2's in-flight `services/products/product-drafts.ts:180` consumes it that way, which confirms the split works for Phase 2.

**[IMPORTANT] Omitted `description` collapsed to `null` — CLOSED.** `productDescriptionSchema` is now `productDescriptionValueSchema` (no internal `.optional()`), composed as `productDescriptionValueSchema.optional()` in `productDraftPatchRequestSchema:252`. New tests cover omitted-key-stays-omitted, explicit `null` clears, `{}` stays empty, and `undefined` rejected for the value form (`packages/shared/src/schemas/product.test.ts`, 37 tests in that file now). Re-verified independently: parsing `{ name: 'x' }` no longer yields a `description` key.

**[IMPORTANT] One-open-edit failed open — CLOSED (with a residual, below).** `patch.ts:33-51` sets `isLegacy: false` explicitly and maps `P2002` to a 409 `conflict` instead of a 500. Two new tests prove it through the real route, including a genuine concurrency race: two parallel `PATCH`es from one caller yield exactly `[202, 409]` and exactly one row (`products-patch.test.ts:38-70`).

**[IMPORTANT] Migration B preflight incomplete — CLOSED.** The `DO` block now aborts on `product_creation.mode <> 'off'` plus every DB-checkable creator-submission marker: `submitted_at`, `moderated_at`/`moderated_by_user_id`, `version > 1`, any `product_photos` row, any non-legacy `product_edits` row. Each abort has its own integration test, plus a negative test proving a *legacy* `product_edits` row does **not** abort (`products-schema.test.ts`, 6 new cases). The misleading "the one precondition that IS DB-checkable" comment is gone.

**[MODERATE] Reorder had no legal intermediate state — CLOSED.** New migration `20260730044500_make_photo_position_deferrable` drops and re-adds both unique constraints as `DEFERRABLE INITIALLY IMMEDIATE` (a new migration file — the already-applied A2 was **not** edited, so no Prisma checksum breakage; `migrate status` clean on both DBs confirms). Verified empirically on my scratch DB: with the constraint deferred, the two-statement swap that previously failed `23505` now commits. Regression test added (`products-schema.test.ts:215-254`) that asserts both halves — immediate fails, deferred succeeds.

**[MODERATE] Photos not ordered by position — CLOSED.** `serializer.ts:40` sorts a copy by `position` before mapping; test feeds `[2, 0, 1]` and asserts `[0, 1, 2]`.

## New issues introduced by the remediation

**[IMPORTANT — NEW] `request_changes` now permanently dead-ends the creator: the product becomes un-editable forever.** The partial unique index treats `changes_required` as an *open* edit (`WHERE (NOT is_legacy AND status = ANY(ARRAY['draft','pending','changes_required']))`, migration A2), and `patch.ts` now converts that collision into a 409. Verified in rolled-back transactions on my scratch DB: with one `changes_required` edit present, a second open edit for the same `(product, submitter)` is **BLOCKED** `P2002 product_id,submitted_by`; with a `rejected` edit present it is **ALLOWED**. In the committed tree the only `ProductEdit` writers are `patch.ts` (create) and `pending-resolve.ts` (update) — `git grep -n "productEdit\." HEAD -- api/src`. So after an admin requests changes: the creator's next `PATCH /v1/products/:id` returns 409 forever (no route updates or deletes an existing edit), and the admin cannot re-resolve it either (`pending.ts:11` lists only `status: 'pending'`; `pending-resolve.ts:16` rejects anything not `pending` with "Already resolved"). Before `6c40854` the `rejected` write freed the slot, so this is a behavioural regression on a live endpoint that `product_creation.mode` does **not** gate. Recovery requires DB surgery. — **Recommendation:** in `patch.ts`, when an open edit already exists for this creator+product with status `draft|changes_required`, *update* it (new `proposed`, status back to `pending`, clear `moderationNotes`) instead of 409, and keep 409 only for `pending` (genuinely under review). Add a test for the full loop: patch → request_changes → patch again → 202.

**[IMPORTANT — NEW] Making the position constraints `DEFERRABLE` silently disables `ON CONFLICT`/Prisma `upsert` on both photo tables.** PostgreSQL refuses a deferrable constraint as an `ON CONFLICT` arbiter. Verified on the scratch DB: `INSERT INTO product_photos … ON CONFLICT (product_id, position) DO NOTHING` → SQLSTATE **55000** `ON CONFLICT does not support deferrable unique constraints/exclusion constraints as arbiters`; and `prisma.productPhoto.upsert({ where: { productId_position: … } })` fails with the same 55000 wrapped in a raw `ConnectorError` — **`err.code` is `undefined`**, so the usual `if (err.code === 'P2002')` handling (the pattern `patch.ts` just adopted) will not classify it and the request surfaces as a 500. `product_edit_photos_product_edit_id_position_key` has the same property. Nothing in the suite covers this, and it typechecks fine. Phase 3 is the natural place to hit it (upload/replace photo at position N). — **Recommendation:** record the prohibition in the Phase 3 phase file ("never `upsert`/`ON CONFLICT` on `(product_id, position)` / `(product_edit_id, position)`; use explicit find-then-create/update inside the reorder transaction") and add a test asserting the constraint is deferrable *and* that the service does not rely on upsert. This is an accepted trade-off of the fix I recommended, not an error in it — but it must be written down before Phase 3 builds on it.

**[MODERATE — NEW] The committed migration-B header still tells the operator to run `prisma migrate deploy` — the exact instruction that caused the incident.** `api/prisma/deferred-migrations/20260730040000_classify_report_hidden_products/migration.sql` (as committed in `d929a56`) says "Phase 8 owns running `prisma migrate deploy` for this file", while the new `README.md` in the same directory says the opposite in step 4 ("do not run `prisma migrate deploy`/`migrate dev` for this step"). A corrected header exists **only as an uncommitted working-tree edit** (`git diff -- api/prisma/deferred-migrations/` — comment-only, points at the README). Given that a stale procedural comment already produced one incident, contradictory instructions must not remain the committed state. — **Recommendation:** commit that header edit.

**[MODERATE — NEW] "Request Changes" kept `variant="destructive"`.** `apps/admin/.../pending-actions.tsx:64-70` still renders the action with the destructive (Alert Red) variant, but requesting changes is resumable feedback, not destruction. `CLAUDE.md`/`docs/design-guidelines.md` reserve Alert Red for expired/destructive state only. Cosmetic and Phase 6 owns the real console — worth one line now so it does not get inherited.

**[INFORMATIONAL — correction to my own earlier claim] The vendored `dist` is *not* byte-reproducible, so "byte-identical" is not a usable drift gate.** Re-running `tsc -p tsconfig.build.json --outDir /tmp/sharedbuild2` and diffing against `apps/mobile/local-packages/@expyrico/shared/dist`: **every `.js` file is byte-identical** (`cmp` over all of them), but 13 `.d.ts` files differ — including files untouched since `1a42b0c` (`schemas/user.d.ts`, `auth.d.ts`, `admin/users.d.ts`, …). Inspection shows the differences are purely member/union **ordering** inside emitted zod inferred types (e.g. `sort: "createdAt" | "email"` vs `"email" | "createdAt"`), i.e. semantically identical declarations. My `1a42b0c` note that the dist was byte-identical was luck, not a guarantee; the meaningful gate is the mobile resolved-import assertion (`apps/mobile/src/tests/shared-contract.test.ts`, 5/5 green), and that is intact.

## Residual items (previously reported, deliberately not fully closed)

- **`is_legacy` still defaults to `true` at the DB level** (verified read-only on `pantry`: `column_default = 'true'`). dev-1 fixed the live writer and proved it with a race test rather than flipping the default, so the invariant is still opt-in for *future* writers. Phase 4 adds more `ProductEdit` writers — recommend a one-line `ALTER COLUMN is_legacy SET DEFAULT false` (+ `@default(false)`) migration before then, so omission fails closed.
- Untouched and still open from the original review (all MODERATE, all correctly deferred): `productSchema.photos` has no `.max(5)`/unique-position rule; Phase 1 compatibility reader for `report_hidden` is unimplemented in `lookup.ts`/`get.ts` (Phase 2 owns it — dev-2's in-flight work appears to be addressing exactly this); rejected-photo storage-key cleanup; admin↔API deploy ordering; uploader `RESTRICT` FKs vs account erasure; the `prisma format` reflow.

## Re-verification verdict

All 1 CRITICAL + 5 IMPORTANT findings are **CLOSED with tests I re-ran myself**, and both decide-now MODERATEs (reorder deferrability, photo sort) are **CLOSED**. The remediation is high quality: it fixed the reported defects at the contract level rather than patching call sites, and added real negative/race tests for each. **Not clean, however:** two new IMPORTANT issues were introduced (creator dead-end after `request_changes`; deferrable constraints breaking `upsert`/`ON CONFLICT` with an unclassifiable error) plus two MODERATEs. The dead-end is the only one that affects a live endpoint today and should be fixed before Phase 4 moderation work lands.

---

# Re-verification round 2 — commit `ced0e72`

## Verification performed

Provisioned a fresh isolated database (`psql -c CREATE DATABASE`, `CREATE EXTENSION pg_trgm`, `psql -f` over all **23** migration files in sort order), ran with `TEST_DATABASE_URL`, dropped it after (`pantry_rev2_1n3u2ahx`). No `prisma migrate deploy/dev/reset` run; `pantry` read only.

| Check | Result |
|---|---|
| Full api suite, isolated DB | **561/561 pass, 88/88 files, zero flakes** — matches dev-1's claim exactly |
| Shared suite / typecheck | 57/57 pass; clean |
| api / admin typecheck | clean (admin catches an invalid `variant`, so the new `accent` value is real) |
| Mobile `shared-contract` | 5/5 pass |
| `prisma migrate status`, read-only, `pantry` **and** `pantry_test` | both "23 migrations found… Database schema is up to date!" — no pending, no applied-but-missing |
| `is_legacy` default, read-only, both DBs | `column_default = 'false'` on both |
| Classify migration still ungated | absent from `_prisma_migrations` on both DBs; only `…_make_photo_position_deferrable` and `…_default_product_edits_is_legacy_false` recorded for 2026-07-30 |
| `pantry` data | `active=3`, zero `pending`/`report_hidden`; `product_edits` empty on both DBs (so the default flip could not have relabelled history; the backfill=`true` path stays covered by the scratch-DB upgrade fixture) |
| Working tree for dev-1's files | clean (`git status -- api/prisma api/src/routes/products/patch.ts apps/admin`) → the migration-B header fix is genuinely committed, not a stray diff |

## Finding-by-finding

**[IMPORTANT] Creator dead-end after `request_changes` — CLOSED.** `patch.ts:33-58` looks up the caller's own open edit (`submittedBy: req.user!.id`, `isLegacy: false`, `status: { in: ['draft','changes_required'] }`) and updates it in place to `pending` with `moderationNotes: null`, only falling through to `create` (+ `P2002` → 409) when none exists. Ownership is scoped correctly — no cross-user edit can be hijacked. Two new tests, both of which I ran as part of the 561: a sequential second patch while the first is genuinely `pending` still 409s, and the full loop patch → `request_changes` → patch → 202 keeps **one** row (same `editId`), clears feedback, reappears in the admin queue, and the final approve applies "Revised proposal" to the product.

**[IMPORTANT] Deferrable constraints vs `upsert`/`ON CONFLICT` — CLOSED (documented).** `phase-03-product-media-pipeline-and-vps-delivery.md` now carries it in both Requirements and Task 3, with the SQLSTATE `55000` text, the `err.code === undefined` caveat, and an explicit "must never call `upsert`/`ON CONFLICT` against `(product_id, position)` / `(product_edit_id, position)`" instruction plus the reason not to revert the deferrability. That is the right place — Phase 3 cannot miss it while writing the photo service.

**[MODERATE] Contradictory migration-B header — CLOSED.** The corrected header (pointing at `README.md`, no `prisma migrate deploy` instruction) is committed in `ced0e72`; working tree clean for that path.

**[MODERATE] Alert Red on a non-destructive action — CLOSED.** `pending-actions.tsx:65` now uses `variant="accent"`, and `button.tsx:12-15` adds that variant as `bg-accent text-accent-foreground` with a comment reserving `destructive` for actual destruction. Token verified real, not a dead class: `apps/admin/tailwind.config.ts:18-21` maps `accent`/`accent-foreground`, and `globals.css:19-21` defines `--accent: 37 91% 55%` ≈ `#F5A623` — the Honey accent from the brand palette, which is the correct semantic for "needs attention".

**[Residual] `is_legacy` fail-open default — CLOSED.** New migration `20260730052600_default_product_edits_is_legacy_false` does `ALTER COLUMN is_legacy SET DEFAULT false`; `schema.prisma:408` mirrors it with `@default(false)`; both live DBs verified. New test proves the fail-closed behaviour *without* passing `isLegacy` at all (`products-schema.test.ts:484-495`), which is exactly the gap the previous round's tests left. The upgrade fixture was extended to replay this migration too, so the historical `is_legacy = true` backfill is still asserted.

## New issues (round 2)

**[MODERATE — NEW] The in-place resubmission is an unguarded read-then-write, so a concurrent proposal is silently lost (and a future Phase 4 writer could be overwritten).** `patch.ts:36-58` does `findFirst` then `update({ where: { id } })` with no status predicate and no transaction. Two concurrent resubmissions from the same creator both read the same `changes_required` row, both update it, both return **202** — one proposal is discarded with no signal to the client. There is no admin-side transition out of `draft|changes_required` today (`pending.ts:11` lists only `pending`; `pending-resolve.ts:16` rejects non-`pending`), so nothing worse is reachable *yet* — but Phase 4 adds `rebase`/`supersede` writers that move edits out of those states, and this write would then resurrect a superseded edit into `pending`. — **Recommendation:** make it a conditional write — `updateMany({ where: { id, status: { in: ['draft','changes_required'] } }, … })` and treat `count === 0` as the 409 path (or re-read inside a transaction). Three lines, and it removes the trap before Phase 4 lands.

**[MODERATE — NEW] Resubmission does not refresh moderation-lifecycle metadata.** Probed on the isolated DB by replaying exactly the route's `update` against a row in the post-`request_changes` state (`resolvedBy`/`resolvedAt` set, product at `version = 3`): after the update the row is `status: 'pending'`, `moderationNotes: null` — correct — but `resolvedBy_cleared: false`, `resolvedAt_cleared: false`, `submittedAt: null`, `baseProductVersion: 1` (vs product `version: 3`), `version: 1`, `createdAt` unchanged. Consequences: (a) `adminProductEditRowSchema` exposes `resolvedBy`/`resolvedAt`, so the admin queue shows an awaiting-review row stamped "resolved by <admin> at <time>", and any Phase 4/6 logic reading `resolvedAt != null` as "already resolved" will misclassify it; (b) `submittedAt` is never written by any code path (`grep` shows `pending.ts:25` reads it, nothing writes it) and the queue orders by `createdAt desc`, so a revision keeps its original queue position and its actual (re)submission time is unknowable; (c) `baseProductVersion` stays at the create-time default while the product moves on, which is precisely the input Phase 4's stale-revision rebase/supersede decision depends on. (b) and (c) predate this commit; the resubmission path makes them materially worse because one row now spans several moderation rounds. — **Recommendation:** clear `resolvedBy`/`resolvedAt`, set `submittedAt: new Date()`, and set `baseProductVersion` to the current `product.version` on both the create and the resubmit paths; if Phase 4 would rather own that, record it there explicitly so its staleness model does not assume these fields are maintained.

## Round-2 verdict

**Clean at IMPORTANT and above.** Both round-1-introduced IMPORTANT issues, both MODERATEs, and the `is_legacy` residual are closed, each with a test that fails without the fix, and the whole suite is green on an isolated database (561/561, no flakes). Phase 1 is landable. Two new MODERATEs remain, both inside the `ProductEdit` moderation lifecycle: the unguarded resubmission write is worth the three-line conditional now, and the stale `resolvedBy`/`resolvedAt`/`submittedAt`/`baseProductVersion` metadata should be assigned to Phase 4 explicitly rather than left implicit.
