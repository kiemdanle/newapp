import type { FastifyInstance } from 'fastify';

// Routes registered in Phase G — placeholder keeps server.ts importable while
// reviews routes (Phase F) ship first. createReportRoute lands in G2.
export async function reportsRoutes(_app: FastifyInstance) {
  // intentionally empty until Phase G2
}
