---
# App role: media directories, systemd units, sudoers, deploy SSH key, backup
# cron, and the UptimeRobot operator reminder (amendment 5 — never automated).
#
# Media directories are provisioned FIRST, before the systemd unit is
# installed/started: Phase 3's config validation fails fast at boot if
# MEDIA_ROOT doesn't already exist and isn't writable, so an API version that
# validates media config must never start against a root Ansible hasn't
# created yet (see infra/README.md's deployment-ordering note).
#
# Least-privilege split (reviewer-p7 II5): private/ and quarantine/ are
# 0700 — owner (app_user) only, no group or "other" bits at all — so nginx
# (running as www-data) cannot read them regardless of what group it is or
# is ever added to. A previous version put every subtree at group-readable
# 0750 and added www-data to app_group "for public read access", which
# actually granted www-data read access to private/ and quarantine/ too —
# the filesystem backstop behind the CDN vhost's single public-only
# `location` block was gone even though nothing was leaking through nginx
# config alone. public/ instead gets its OWN group (www-data) with the
# setgid bit, so www-data can read it directly without ever joining
# app_group, and every file/directory the API creates underneath it at
# runtime automatically inherits group=www-data (POSIX setgid propagation)
# without Ansible needing to know about each dynamically-created path.
# media_root itself only needs `--x` (traversal, not listing) for "other" so
# www-data can reach into public/ without any group membership there either.
- name: Ensure the media root is traversable (not listable) by any process
  ansible.builtin.file:
    path: "{{ media_root }}"
    state: directory
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0751"

- name: Ensure private/ and quarantine/ are owner-only, unreadable by nginx
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0700"
  loop:
    - "{{ media_root }}/private"
    - "{{ media_root }}/private/products"
    - "{{ media_root }}/quarantine"

- name: Ensure public/ is readable by nginx via its own group, with setgid so runtime-created content inherits it
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: "{{ app_user }}"
    group: www-data
    mode: "02750"
  loop:
    - "{{ media_root }}/public"
    - "{{ media_root }}/public/products"

- name: Install pantry-api systemd unit
  ansible.builtin.template:
    src: pantry-api.service.j2
    dest: /etc/systemd/system/pantry-api.service
    mode: "0644"
    owner: root
    group: root
  notify:
    - Reload systemd
    - Restart pantry-api

- name: Install pantry-admin systemd unit
  ansible.builtin.template:
    src: pantry-admin.service.j2
    dest: /etc/systemd/system/pantry-admin.service
    mode: "0644"
    owner: root
    group: root
  notify:
    - Reload systemd
    - Restart pantry-admin

# Amendment 2: restart-only sudoers. The units are Type=simple with no
# ExecReload, so `systemctl reload` would be a silent no-op. Deploys use
# `systemctl restart` which sends SIGTERM and lets the process drain within
# TimeoutStopSec=30. The fragment grants NOPASSWD on exactly six commands —
# restart / start / stop for each unit, no wildcards, no broader sudo.
- name: Install pantry sudoers fragment (validated with visudo)
  ansible.builtin.template:
    src: sudoers-pantry.j2
    dest: /etc/sudoers.d/pantry
    mode: "0440"
    owner: root
    group: root
    validate: visudo -cf %s

- name: Ensure app user .ssh directory exists for {{ app_user }}
  ansible.builtin.file:
    path: "/home/{{ app_user }}/.ssh"
    state: directory
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0700"

- name: Install GitHub Actions deploy authorized_keys
  ansible.builtin.copy:
    content: |
      # Managed by Ansible. Used by .github/workflows/deploy.yml to ssh in as
      # the {{ app_user }} user. Update group_vars/all.yml deploy_ssh_authorized_key
      # then re-apply with --tags app to rotate.
      {{ deploy_ssh_authorized_key }}
    dest: "/home/{{ app_user }}/.ssh/authorized_keys"
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0600"
  when: deploy_ssh_authorized_key | length > 0

- name: Ensure pantry-api enabled
  ansible.builtin.service:
    name: pantry-api
    enabled: true

- name: Ensure pantry-admin enabled
  ansible.builtin.service:
    name: pantry-admin
    enabled: true

- name: Install nightly backup cron at 03:17 UTC
  ansible.builtin.cron:
    name: pantry-backup
    user: root
    hour: "3"
    minute: "17"
    job: "{{ app_root }}/current/infra/scripts/backup.sh >> /var/log/pantry/backup.log 2>&1"

# ---------------------------------------------------------------------------
# UptimeRobot reminder (amendment 5)
#
# External uptime monitoring is intentionally NOT automated: it must keep
# running when the host is down, so it cannot live on the host. The deploy
# operator must — manually — create an UptimeRobot HTTP monitor on
# https://{{ api_domain }}/health with a 5-minute interval and an email alert
# contact, AFTER the first successful deploy.
#
# See infra/README.md → "Post-deploy: external uptime monitor".
# ---------------------------------------------------------------------------
- name: Remind operator to wire UptimeRobot /health monitor
  ansible.builtin.debug:
    msg: >-
      MANUAL STEP after first deploy: create an UptimeRobot HTTP monitor on
      https://{{ api_domain }}/health (5-minute interval, email alert contact).
      See infra/README.md "Post-deploy: external uptime monitor".
