# Admin IP allowlist snippet (placeholder).
#
# This file is intentionally a comment-only stub for the FIRST deploy. The
# admin vhost reads `admin_allowlist` from group_vars/all.yml directly via
# Jinja, so this snippet is informational documentation for operators.
#
# Once you have a stable operator egress IP (or VPN range), add CIDRs to
# group_vars/all.yml:
#
#   admin_allowlist:
#     - 203.0.113.42/32        # office wifi
#     - 198.51.100.0/24        # staff VPN
#
# Then re-apply:
#
#   ansible-playbook -i inventory.ini site.yml --tags nginx
#
# DO NOT add CIDRs here — they belong in group_vars/all.yml so the deploy
# topology is reviewable in git.
