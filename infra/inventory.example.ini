# Copy this file to inventory.ini and fill in the host details before running
# the playbook. Do NOT commit inventory.ini.
#
# The SSH key path below assumes the controller has the deploy operator's key
# installed at ~/.ssh/pantry_vps_ed25519 (mode 0600). Adjust if you store it
# elsewhere.

[linhkienkts]
pantry ansible_host=135.181.15.215 ansible_user=root ansible_port=22 ansible_ssh_private_key_file=~/.ssh/pantry_vps_ed25519

[linhkienkts:vars]
ansible_python_interpreter=/usr/bin/python3
